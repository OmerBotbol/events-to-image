from utils import decoder
from pathlib import Path

## First we need to convert the events into a .csv file, uncommend the next 3 lines to do so and make sure that the events data seats in the "events" folder
  
# script_dir = Path(__file__).parent
# path = script_dir / "events" / "experiment_0" # change the last string to the events' folder 
# decoder.runDecoder(path)

